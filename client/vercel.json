{
  "framework": "vite",
  "buildCommand": "cd client && pnpm install && pnpm run build",
  "outputDirectory": "client/dist/public",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
